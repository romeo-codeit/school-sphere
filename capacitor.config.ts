import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.romeocodeit.schoolsphere',
  appName: 'OhmanFoundations',
  webDir: 'dist/public'
};

export default config;
